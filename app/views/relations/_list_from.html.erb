<% @relations = @taggable.relations_from

   relationTypes = [];
   
   @relations.each do |r| 
     if !relationTypes.member?(r.type)
        relationTypes << r.type
     end
   end
  
  relationTypes.each do |rt|
    @taggables = @taggable.related_from( rt ) 
    %>
    <%= rt %>:
    <table>

    <%= render :partial=>"taggables/list" %>

    </table>  

  <% end %>
