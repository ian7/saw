<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title>Artifacts: <%= controller.action_name %></title>
  <%= stylesheet_link_tag 'saw' %>
  <!--<script type="text/javascript" src="/javascripts/jquery.tools.min.js"></script>-->
  <%= javascript_include_tag 'jquery.tools.min' %>
  <script>
  $.noConflict();
  </script>
  
</head>
<body <%= " onload='#{@onload}'"%>>
	
<%#= image_tag"background.png", :id => 'background' %>
<%#= image_tag"background_bottom.png", :id => 'background_bottom' %>

<p style="color: green"><%= flash[:notice] %></p>

<%= render :partial => "layouts/ribbon", :object=>@issues %>

<div id="center">

<%= yield %>

<%= render :partial => "layouts/footer" %>

</div>
<!-- overlayed element --> 
<div class="overlay" id="overlay"> 
 
    <!-- the external content is loaded inside this tag --> 
    <div class="contentWrap"></div> 
 
</div>

<!-- make all links with the 'rel' attribute open overlays -->
<script>
jQuery(function() {

	// if the function argument is given to overlay,
	// it is assumed to be the onBeforeLoad event listener
	jQuery("a[rel]").overlay({

		//expose: { 
        //color: '#fff', 
        //loadSpeed: 200, 
        //opacity: 0.8 
    //},
		effect: 'apple',

		onBeforeLoad: function() {

			// grab wrapper element inside content
			var wrap = this.getContent().find(".contentWrap");

			// load the page specified in the trigger
			wrap.load(this.getTrigger().attr("href"));
		}

	});
});
</script>
</body>
</html>
