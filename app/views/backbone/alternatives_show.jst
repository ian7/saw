    		<td>
	    	 	<%=a.get('id')%>
	    	</td>
	   		<td>	   		
	    		<b>Name:</b>
	    		<span class="name" contenteditable="true"><% if(a.get('name')) {%><%= a.get('name') %><% } else { %>(empty)<% } %></span>
		    </td>
	    	<td>
				<% if( !a.isNew() ) {  %>
<!--					<div class="button gray unrelateAlternative">UnRelate</div> -->
		    		<div class="button gray deleteAlternative">Delete</div>
				<% } %>
	    	</td>
	    	<td> 
		    	Decide: <% _(a.attributes.decisions).each(function(decision) { %>
		    		<b onClick="App.Helpers.Items.decide('<%=a.attributes.relation_url%>','<%=decision.decision_tag_id%>');"><%= decision.name.charAt(0) %>:</b> <%= decision.count %>, 
		    	<% });%>
	    	</td>