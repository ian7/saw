<ul>
    <% _(tags).each(function(tag) { %>
    <li>
    	<b>Type:</b> <%= tag.attributes.type %>, <b>Name:</b> <%= tag.attributes.name %> (tagging: <%=tag.attributes.tagging_id %>) <b onClick="App.controller.unTag('<%=tag.attributes.tagging_id%>');">REMOVE</b> 
    </li>
    <% }); %>
</ul>
