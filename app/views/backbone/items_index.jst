    	<span class="e6" contenteditable="true"><%= item.get('name') %></span>
		<% if( !item.isNew() ) { %>
			<a href=#/<%= item.get('id')%>>details</a>,
	    	<div class="button gray expand">Expand</div>,
	    	<div class="button gray deleteItem">Delete</div>.		
		<table class="alternativeList"></table>
	   <% } %>
