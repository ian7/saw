<h3>
	<a href='#'>BACK !</a>	
</h3>

<% _(item.attributes).each(function(value, key) { 
	if (key.charAt(0) != '_' && key != "url" ) { %>
<li>
<b> <%= key %>: </b><i class="edit5" id="<%=key%>"><% if( value ) { %><%= value %><% } else { %>(empty)<% } %></i>
</li>
<% }; 
});%>
<hr>
<a href="#<%=item.id%>" onClick="App.controller.addTag();">New tag</a>